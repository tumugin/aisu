mutation {
  userLogin(
    params: { email: "aoisuzu@example.com", password: "aoisuzuuu" }
  ) {
    userCreatedAt
    userEmail
    userEmailVerifiedAt
    userId
    userName
    userUpdatedAt
  }
}
