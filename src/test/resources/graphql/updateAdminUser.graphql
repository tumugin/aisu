mutation UpdateAdminUser($adminUserId: ID!, $params: UpdateAdminUserParamsInput!) {
  admin {
    adminUser {
      updateAdminUser(adminUserId: $adminUserId, params: $params) {
        adminUserCreatedAt
        adminUserEmail
        adminUserId
        adminUserName
        adminUserUpdatedAt
      }
    }
  }
}
