mutation AddIdolToGroup($groupId: ID!, $idolId: ID!) {
  group {
    addIdolToGroup(groupId: $groupId, idolId: $idolId) {
      groupCreatedAt
      groupId
      groupName
      groupStatus
      groupUpdatedAt
      user {
        userId
        userName
      }
      userId
    }
  }
}
