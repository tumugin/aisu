mutation AddGroup($params: AddOrUpdateGroupParamsInput!) {
  group {
    addGroup(params: $params) {
      groupCreatedAt
      groupId
      groupName
      groupStatus
      groupUpdatedAt
      user {
        userId
        userName
      }
      userId
    }
  }
}
