mutation {
  userCreate(
    params: {
      email: "aoisuzu@example.com"
      name: "藍井すず"
      password: "aoisuzu"
    }
  ) {
    userCreatedAt
    userEmail
    userEmailVerifiedAt
    userId
    userName
    userUpdatedAt
  }
}
